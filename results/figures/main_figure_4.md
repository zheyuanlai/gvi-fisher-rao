# main_figure_4 caption draft

Applications. (a) Best objective gap at a fixed oracle budget over the full shifted log-cosh grid ($d\in\{2,10,50\}$, $\kappa_{\rm base}\in\{1,10,10^2\}$, $\rho\in\{0.1,1,5\}$), each method at its own best swept step, plotted against the affine-invariant condition number $\kappa_\star$; marker size encodes the dimension. (b) Bayesian logistic regression with a proper Gaussian prior, deterministic methods at their own best swept step. (c) The stochastic methods at $B=16$, each run at a quarter of the largest step its own deterministic counterpart was measured to tolerate on that cell, so no method is placed outside its stable range. The non-iterative Laplace baseline is shown for reference.

Underlying data: `results/processed/main_figure_4.csv`.
